# Copy to .env and adjust. Every setting here is read in src/config/env.ts.

# The port the API listens on. Defaults to 4000.
PORT=4000

# development | test | production. Defaults to development.
NODE_ENV=development

# DATABASE_URL joins this in task 0.15, with PostgreSQL and Prisma.
